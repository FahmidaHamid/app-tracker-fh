import React from "react";

const ResetSearch = () => {
  return (
    <>
      <button onClick={() => {}} className="btn btn-accent">
        Reset Search Keys
      </button>
    </>
  );
};

export default ResetSearch;
